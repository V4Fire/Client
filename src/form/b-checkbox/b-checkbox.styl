/*!
 * V4Fire Client Core
 * https://github.com/V4Fire/Client
 *
 * Released under the MIT license
 * https://github.com/V4Fire/Client/blob/master/LICENSE
 */

@import "super/i-input/i-input.styl"

$p = {
	width: 1.2em
}

b-checkbox extends i-input
	display inline-block

	&__wrapper
		display flex
		align-items center
		cursor pointer

	&__box
		display block
		width $p.width
		height @width
		cursor pointer

		&:after
			content ""
			position absolute
			display none

	&_checked_true &__box:after
		display block
		left ($p.width / 2.5)em
		top ($p.width / 6)em
		width ($p.width / 6)em
		height ($p.width / 2)em
		transform rotate(45deg)
